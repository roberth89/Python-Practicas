
import subprocess
import io

#Esto desconecta el internet
#subprocess.call(["ipconfig", "/release"], shell=True)

#for i in range(6):
 #   subprocess.call(["ipconfig", "/release"], shell=True) # release renew flushdns
